#include<bits/stdc++.h>
using namespace std;
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int a[10];
    memset(a, 0, sizeof(a));
    for (int i = 0; i < 10; i++)
    {
        cout<<a[i]<<" ";
    }
}